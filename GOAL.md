# ty nondeterminism investigation

Goal: find distinct sources of nondeterministic behavior in `ty`, fix each confirmed issue, and open one focused PR in `jelle-openai/ruff` per issue.

## Working rules

- Prefer a small reproducer before changing code.
- Treat output ordering, inference results, diagnostics, snapshots, and incremental behavior as possible nondeterminism surfaces.
- Treat nondeterminism in whether ty panics as in scope, but not exact output differences when repeated runs raise essentially the same panic; those panics should be fixed separately from this investigation.
- Keep each PR focused on one root cause with a regression test.
- Put the exact nondeterministic behavior and the root cause in each PR body, not just the symptom or chosen fix.
- Update this file with confirmed patterns, useful commands, and ruled-out leads as the investigation progresses.

## Upstream transfer workflow

Only run this after the user gives an explicit OK to transfer a specific PR to `astral-sh/ruff`.

1. Re-read the focused `jelle-openai/ruff` PR and nearby ty history or PR discussion for similar fixes, then adjust the change if needed so the implementation and regression test match existing ty style.
1. Start from the latest `upstream/main`, rebase or recreate the focused branch there, and verify the diff contains only the issue-specific change intended for the main repo.
1. Remove investigation-only or fork-only changes before transfer, including `GOAL.md` and any notes that belong only in `jelle-openai/ruff`.
1. Run the focused regression, the relevant crate test suite, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `uvx prek run --files ...`, and any extra validation appropriate for the touched paths.
1. Push the cleaned branch to the fork, open a draft PR against `astral-sh/ruff` with a concise root-cause description and `ty` label, then verify GitHub CI passes before calling the transfer done.

## Initial leads

- Recent history contains ty fixes for deterministic type, constraint, overload, and synthesized-field ordering.
- Search `crates/ty*` for iteration over hash-based collections that can influence user-visible output or semantic tie-breaking.
- Check GitHub PR discussion around prior nondeterminism fixes for nearby unfinished cases.

## Investigation notes

- Prepared the current `scikit-build-core` primer project under `/private/tmp/scikit-build-core-py311-nondet` while following upstream issue `astral-sh/ty#1389`.
- Reproduced a current `scikit-build-core` flake with the primer's Python 3.11 environment: `TY_MAX_PARALLELISM=8 ty check ... src tests noxfile.py` alternated between 75 and 77 diagnostics, while `TY_MAX_PARALLELISM=1` was stable at 77 diagnostics.
- Existing inference tests use a useful perturbation pattern: type-check an unrelated file first, then inspect the target file after Salsa has cached and interned a different path.
- Confirmed a current `dd-trace-py` primer flake after `95eec58af2`: twelve `TY_MAX_PARALLELISM=8` full-project runs on unpatched `main` produced two concise diagnostic hashes, and the differing diagnostic came from `SubprocessCmdLine.arguments`.
- Minimized that flake to a full-scope `new_args = []` collection inside an implicit instance attribute cycle. Priming `new_args` before reading `SubprocessCmdLine("").arguments` changed the inferred `arguments` type, so query entrypoint was affecting the fixed point.
- Exact nondeterminism: the same `dd-trace-py` source sometimes inferred `SubprocessCmdLine.arguments` as `list[Unknown | str]` and sometimes as `list[_T@deque | str]`, changing the emitted diagnostic text. Parallel file checking changed which query first entered the shared implicit-attribute/full-scope-collection Salsa cycle.
- Root cause: a later collection use could feed a typevar owned by an inner generic call such as `collections.deque(...)` back into full-scope collection inference. That out-of-scope typevar was not meaningful in the collection literal's enclosing generic scope, but whether it escaped or collapsed to `Unknown` depended on the cycle entrypoint.
- Fix direction for the first PR: replace typevars not bound by an enclosing generic context with `Unknown` before using later-use constraints for full-scope collection literals; keep in-scope generic typevars intact.
- Published the first confirmed fix as `jelle-openai/ruff#3`.
- PR `#3` test note: the Rust query-entrypoint test belongs in `types/infer/tests.rs`, but its dd-trace-faithful inline Python fixture was too large. A reduced cache/implicit-attribute/full-scope-collection repro now keeps only the pieces needed for the same pre-fix equality failure while still passing with the fix.
- Removed `dd-trace-py` from the flaky primer list in that PR after eight final parallel concise checks produced the same diagnostic hash.
- The `scikit-build-core` flake was not semantic inference: missing runs indexed 173 files while present runs indexed 185, omitting tracked `src/scikit_build_core/build/*.py` files before diagnostics could be checked.
- Exact nondeterminism: `ty check ... src tests noxfile.py` sometimes included source `build` packages and emitted 77 diagnostics, and sometimes excluded those same tracked files and emitted 75 diagnostics. The omitted files contained the two disappearing diagnostics.
- Root cause: the project index passed `src`, `tests`, and `noxfile.py` into one parallel multi-root `ignore` walk. With `.gitignore` containing `tests/**/build/`, the `tests`-scoped ignore rule could sometimes be applied while visiting the source `src/scikit_build_core/build/` package, depending on worker scheduling.
- Dependency follow-up: the same omission reproduces with `rg --threads 12 --files src tests noxfile.py`, so the strongest root appears to be in ripgrep's `ignore` crate rather than ty. `Ignore::add_parents` caches compiled parent matchers only by parent directory even though each cached matcher stores an `absolute_base` derived from the current walk root; whichever parallel root populates that shared cache first can lend its base path to the other root.
- Fix direction for the second PR: keep the Ruff-side workaround focused by walking OS directory roots independently, and separately pursue the underlying `ignore` fix by scoping compiled parent matcher cache entries by both parent directory and walk root.
- Published the second confirmed fix as `jelle-openai/ruff#4`.
- Removed `scikit-build-core` from the flaky primer list in that PR after 100 final parallel concise checks produced the same diagnostic hash.
- Implemented the underlying ripgrep fix locally in `/Users/jelle/code/ripgrep` on branch `jelle/codex/ignore-multiroot-determinism`: scope compiled parent matcher cache entries by both parent directory and walk root. The focused new `ignore` regression failed before the fix and passed after it, and patched `rg --threads 12 --files src tests noxfile.py` kept the source `build` file in 100/100 runs.
- Re-ran the current primer list with eight flaky runs on unpatched current `main`; only the already-confirmed `dd-trace-py` and `scikit-build-core` cases reproduced there. Among formerly flaky projects, `bokeh`, `cloud-init`, `colour`, `ibis`, `jax`, `meson`, `pandas-stubs`, `PyGithub`, `rotki`, `sockeye`, and `vision` were stable for eight prepared-project runs, and `pydantic` was stable for 50 focused runs. An earlier refreshed whole-project `pydantic` probe stack-overflowed in 8/8 runs with only the runtime thread ID changing, so that old output-only variation is ruled out under the scope above. On fresh `upstream/main` at `7c2e9fbc83`, 12 parallel package-path `pydantic` checks exited normally with the same concise hash, while isolated whole-project `pydantic` checks still aborted in 8/8 runs, so `pydantic` currently gives no panic/no-panic lead. A fresh `6aaa91ac2b` replay of the historical `pydantic/fields.py` `dict[str, Divergent]` breadcrumb from `#21595` remained non-panic and kept one exact and normalized concise hash for 100 runs alone plus 100 runs with `pydantic/__init__.py` in each explicit checked-file order, so that old focused breadcrumb is now ruled out too. `core` reproduced the diagnostic output-order root cause described below rather than a new semantic root cause.
- A generated two-file probe exposed a deterministic panic whose Salsa intern IDs and fatal-diagnostic line order varied across parallel runs. Per the project scope above, that is a ruled-out lead unless repeated runs differ in whether ty panics at all.
- Generated-probe coverage so far: identical parallel two-file checks found no in-scope candidate through seed 2999 after skipping runs where both outputs had essentially the same panic; single-threaded file-entrypoint permutations found no in-scope candidate through 450 two-file seeds and 200 three-file import-chain seeds. A fresh `upstream/main` six-module full-project probe found no in-scope candidate through seed 500 after continuing past the original seed-275 stop; the continued slice only added same-panic skips plus generation/check timeouts, not a mixed panic/no-panic or output-hash candidate. A lighter six-module cyclic import/class-reference probe found no in-scope candidate or timeout through seed 4999. Keep widening the probe shape instead of repeating those exact ranges.
- Targeted typed templates for the old hydra generic-union shape, protocol inference, callable-return typevars, multiple return `Callable`s with callable-only typevars, union-call multi-inference around the old `#21649` class, ParamSpec keyword-binder overload seeding, recursive collection inference, same-scope called overloads with recursive inferred returns, final-class abstract-method diagnostics, `__all__` star imports, and the recent `TypedDict | Mapping[...]` dict-compatible fallback shape were stable under file-entrypoint perturbation and repeated parallel checks. A fresh `6aaa91ac2b` typed-template pass added the ParamSpec `wrapper(TagSet=[...], func=put_object)` shape, a multi-return-callable reveal shape adjacent to active upstream `#24949`, a union-call multi-inference reveal shape adjacent to historical `#21649`, and a final abstract-method enumeration shape; it found no candidate across all single-threaded checked-file permutations plus repeated parallel checks for those cases or the existing typed templates. Focused repeats then kept one non-panic exact and normalized concise hash for 100 runs in each explicit checked-file order for the multi-return-callable reveal (`Unknown`), the union-call multi-inference reveal (`Unknown`), and the final-class diagnostic listing unimplemented abstract methods `beta` and `alpha`. The fresh same-scope overload probe adjacent to historical `#20966` emitted its expected single `invalid-assignment` diagnostic and kept one non-panic exact and normalized concise hash for 100 runs in each explicit checked-file order on `6aaa91ac2b`. The fresh TypedDict fallback probe on `6aaa91ac2b` emitted the expected three diagnostics for its intentionally bad literal and kept one non-panic exact and normalized concise hash for 100 runs in each explicit checked-file order.
- Fresh-upstream multi-file cycle probes around the mdtest robotframework decorator regression and a pack of `Cached.metadata`, implicit-attribute, linked-list narrowing, and many-attribute cycle cases each kept one non-panic concise hash for 100 parallel full-project runs. These are useful ruled-out leads for the broad cycle-head theory from `astral-sh/ty#1670`, not new PR candidates. Re-reading `#1670` on fresh `upstream/main` `6aaa91ac2b` found no newer concrete breadcrumb beyond the already-followed `#21722` cycle-union experiment and same-revision beartype/scikit/pandas-stubs examples from `#21864`; a sharper direct replay of the issue-thread scikit-build-core `_logging.py:153` breadcrumb with `noxfile.py` kept one non-panic exact and normalized concise hash for 100 runs in each explicit checked-file order, so that live umbrella issue still does not identify a fourth current root.
- A broader fresh-upstream cycle corpus replay copied every current `*cyc*.py` and `*recursive*.py` corpus file into a two-file project whose second checked file imports the target; each kept one non-panic exact and normalized concise hash for 40 parallel runs. Fresh direct reruns of historical `pandas-stubs`, `static-frame`, and `tornado` primer leads likewise kept one non-panic exact and normalized concise hash for 100 parallel full-project runs each, and current `prefect`, `scipy-stubs`, `setuptools`, `strawberry`, `sympy`, and `xarray` primer leads did the same for 40 runs each. Fresh whole-project `scikit-learn` aborted with the same return code and same exact and normalized concise hash in 30/30 runs, so it remains a ruled-out panic/no-panic lead under the scope above.
- Fresh current reruns of `jax`, `meson`, and `vision`, chosen from projects touched by the merged pending-constraint ecosystem diff, each kept one non-panic exact and normalized concise hash for 40 parallel full-project runs; those are ruled-out low-frequency follow-ups for that merged constraint lane.
- `ecosystem-analyzer` checks out the analyzed repository's `origin/main`, so analyzer results are only fresh-upstream results when that remote points at `astral-sh/ruff`. An earlier 20-run continuation in the original worktree therefore only swept stale fork `origin/main` `38562889b9`; it found no new distinct candidate before being stopped at already directly ruled-out slow `pandas-stubs`, but it should not be used as current-upstream evidence. Re-running a curated 41-project continuation in a disposable clone whose `origin/main` was fetched from `astral-sh/ruff` at `7c2e9fbc83` found zero flaky diagnostic locations in 20 runs each. The analyzer still would not catch the known `core` same-line output-order flip.
- Historical LSP panic/no-panic reports around `backdate.rs` (`astral-sh/ty#1565` / `#2378`) point at the already-merged untracked-read fix `#24051`; current `ProjectDatabase` keeps the project handle stable instead of replacing it outside Salsa's dependency graph. Without a fresh current repro, treat that as historical context rather than a new current candidate.
- An eight-run `ecosystem-analyzer` sweep over the current `good.txt` primer list on stale unpatched `origin/main` `38562889b9` re-found only the known `dd-trace-py` flaky diagnostic content. It was only a broad sieve: it tracks diagnostic variants and locations, so it did not catch the known low-frequency `core` output-order flip. Its only abort leads were `scikit-learn` and `scipy`; prepared-project reruns aborted with the same non-unwinding panic in 8/8 runs for each, and fresh `upstream/main` `scipy` did the same in 8/8 runs, so those are ruled-out panic/no-panic leads under the scope above. A fresh-upstream prepared-project sweep over `PyGithub`, `bokeh`, `cloud-init`, `colour`, `ibis`, `jax`, `meson`, `pandas-stubs`, `rotki`, `sockeye`, and `vision` kept one exact and one normalized concise hash for eight runs each; whole-project `pydantic` only re-confirmed its consistent abort lane. A direct fresh-upstream rerun of an old formerly-flaky `rich` checkout kept one non-panic concise hash for 12 parallel full-project runs.
- The original same-revision mypy-primer check in `#21864` only surfaced `beartype`, `scikit-build-core`, and `pandas-stubs`. Fresh-current focused replay of its `beartype/claw/_package/clawpkgtrie.py` breadcrumb now covers both old `:66` and `:247` report locations: the file alone and paired with `beartype/__init__.py` in each explicit checked-file order produced zero diagnostics and kept one non-panic exact and normalized concise hash for 100 parallel runs on `6aaa91ac2b`; the whole-project `beartype` probe was too slow for a heartbeat and was stopped before evidence. The other two same-revision breadcrumbs are already covered above, so that old CI lane does not currently add a new distinct root.
- A history-guided follow-up on older flaky breadcrumbs did not add a current root. `#21524`'s aside about still-flaky file-watching tests did not lead to a current ty checker issue: the live issue/PR search only found that old discussion, and the current watcher tests document backend event-shape differences rather than a same-source type-check result flip. `#22064`'s historical bot diff named `Tanjun` as a callable/constraint-set ecosystem lead; a fresh `6aaa91ac2b` prepared checkout at `88d43a267a` kept one non-panic exact and normalized concise hash for 12 parallel full-project runs, and its old `tanjun/dependencies/data.py:347` `cached_inject` breadcrumb kept one non-panic exact and normalized concise hash for 100 runs in each explicit checked-file order with `tanjun/__init__.py`. A live GitHub issue/PR search after that replay again found no May 27 ty nondeterminism update beyond the already-tracked issue and draft-PR set.
- A direct fresh-upstream invalid-override probe around `check_class` member iteration stayed stable for 100 parallel full-project runs, so the nearby `FxHashSet` iteration is not a confirmed user-visible nondeterminism lead.
- Fresh-upstream reruns of historical constraint/union-order leads did not produce a new current candidate: a direct `pylox` primer checkout kept one non-panic exact and normalized concise hash for 100 parallel full-project runs, and a hand-minimized callable/protocol/typevar constraint probe around the old `DiffeRT` shape did the same for 100 runs. A fresh current `Expression` primer did reproduce non-panic diagnostic-content nondeterminism: repeated parallel checks of `expression/collections/maptree.py README.py` alternated between `MapTreeLeaf[Key@rebalance | Value@rebalance, Value@rebalance]` and `MapTreeLeaf[Key@rebalance, Value@rebalance | Key@rebalance]`, changing whether upper-bound diagnostics were emitted. A local experiment that preserved source insertion order while materializing per-path typevar bounds made the repro stable, and active upstream draft `#22578` also made both the two-file repro and the full project stable for 100 runs, so treat this as confirmation for that in-flight constraint-set root cause rather than a separate fork PR.
- Avoid duplicating active upstream work without a separate reproducer: draft PR `#21722` covers cycle-recovery union output, draft PR `#22578` covers constraint-set `source_order` removal, and PR `#24949` covers deterministic generic callable freshening. Directly testing `#24949` against its bot-diff leads made the fresh-main `Expression` two-file repro stable for 60/60 runs while `setuptools` and `xarray` stayed one hash on both revisions, so `Expression` remains active-upstream overlap rather than a separate fork PR. A fresh `6aaa91ac2b` replay of `#24949`'s remaining memory-report projects did not add a callable-adjacent current candidate either: prepared `sphinx` at `cc7c6f435a`, `trio` at `d66c4fb970`, and `flake8` at `bb943328ef` each kept one non-panic exact and normalized concise hash for eight parallel full-project runs, then kept one non-panic exact and normalized concise hash when their lexicographically first and last tracked Python files were each checked before `.` single-threaded. A live GitHub issue/PR search after that replay again found no May 27 ty nondeterminism update beyond the already-tracked issue and draft-PR set.
- Fresh `upstream/main` advanced to `7c55833109` with dynamic-class generic-base checking, a variance diagnostic help message, and `#25417` changing `flaky.txt` from `pydantic` to `Expression`. Rebuilding `ty` there and rerunning the focused `expression/collections/maptree.py README.py` command reproduced the known non-panic split immediately: 100 parallel runs produced the same two normalized concise hashes 68/32, alternating between `MapTreeLeaf[Key@rebalance, Value@rebalance | Key@rebalance]` with 35 diagnostics and `MapTreeLeaf[Key@rebalance | Value@rebalance, Value@rebalance]` with upper-bound diagnostics and 39 diagnostics. That is the same already-confirmed active `#22578` constraint-set/source-order root now reflected in the upstream flaky list, not a fourth fork PR. The new dynamic-class helper's `FxHashMap` is only keyed lookup/insertion while conflict reporting follows explicit base order, so that new source did not expose a separate raw hash-iteration lead; a follow-up narrow audit of the touched semantic files found variance and generic-context walks using source/definition order, while the remaining diagnostic-adjacent hash containers were the already-tracked project `collect_set` scheduling/index lane and notebook diagnostics grouped by cell URL before per-cell publication. Whole-project `pydantic` on `7c55833109` still aborted in 8/8 runs with eight exact panic hashes but one normalized panic hash, so its removal from `flaky.txt` does not add an in-scope panic/no-panic candidate. A live GitHub issue/PR search after that refresh found only merged `#25417` in the May 27 ty nondeterminism query.
- A compact current-`flaky.txt` matrix on `7c55833109` kept the live list mapped to known lanes rather than uncovering a changed symptom. Focused `Expression` repeated its active `#22578` constraint-set split for 20 parallel runs (13/7 across the same two concise hashes), and the exact `scikit-build-core` `src tests noxfile.py` command repeated the PR `#4` multi-root index split for 20 parallel runs (19/1 across the same present/missing build-folder hashes seen in the saved scikit artifacts). The focused dd-trace `ddtrace/contrib/internal/subprocess/patch.py` target stayed non-panic with one exact and normalized concise hash for 20 runs alone and 20 runs with `ddtrace/__init__.py`; an attempted 20-run dd-trace whole-project matrix exceeded heartbeat scale and was stopped before producing evidence, so it should not be cited as a current full-project rerun. A live GitHub issue/PR search after this matrix again found only merged `#25417` in the May 27 ty nondeterminism query.
- Live issue follow-up on fresh `upstream/main` `7c2e9fbc83` found no new distinct current root: `astral-sh/ty#3541` comments point Prefect at merged variance/`TypeIs` work in `#24927` and `#24928`, matching the already-stable fresh Prefect rerun above, and the issue was closed on 2026-05-27 based on that analysis; `astral-sh/ty#1389` still mentions scikit-build-core's `src/scikit_build_core/build/_wheelfile.py`, and forcing that file explicitly into `ty check ... _wheelfile.py noxfile.py` kept one non-panic exact and normalized concise hash for 60 parallel runs, so the currently observed scikit symptom still folds into the already-confirmed `build`-folder indexing root unless a new forced-file repro appears. A prepared fresh-current `hydra-zen` replay of the historical `ty check src tests/annotations` command kept one non-panic exact and normalized concise hash for eight parallel runs, so `astral-sh/ty#369` is historical context rather than a new current candidate.
- Follow-up from closed PR `#21686` did not uncover a new current root: its discussion correctly treats project-file `FxHashSet` iteration as an unstable API but not itself a confirmed semantic bug, and the old scikit-build-core `_logging.py:153` `unsupported-base` breadcrumb from that PR kept one non-panic exact and normalized concise hash for 100 fresh-current runs when `_logging.py` was forced explicitly into `ty check ... _logging.py noxfile.py`. Fresh-current direct reruns of historical flaky primer projects `artigraph`, `materialize`, and `openlibrary` each kept one non-panic exact and normalized concise hash for eight parallel full-project runs. A sharper multi-file Liskov/Protocol self-cycle template based on the `astral-sh/ty#2507` union-order discussion likewise kept one non-panic exact and normalized concise hash for 100 parallel full-project runs and both explicit checked-file orders. The prepared `pandas` historical replay is now ruled out as a current focused lead on fresh `upstream/main` `6aaa91ac2b`: `pandas/core/generic.py` kept one non-panic exact and normalized concise hash for 60 parallel runs, `generic.py` plus `frame.py` kept one non-panic exact and normalized concise hash for 40 runs in each explicit checked-file order, and the wider `pandas/core` slice kept one non-panic exact and normalized concise hash for eight runs. Whole-project pandas is still too expensive for heartbeat-sized repetition, but the old replay no longer has an unfinished focused breadcrumb.
- A resumed prepared-project ecosystem sweep on fresh `upstream/main` `6aaa91ac2b` found no new distinct current root. `PyGithub`, `bokeh`, `cloud-init`, `colour`, `ibis`, `jax`, `meson`, `pandas-stubs`, `rotki`, `sockeye`, and `vision` each kept one non-panic exact and normalized concise hash for eight parallel full-project runs. Whole-project `pydantic` aborted in 8/8 runs with varying panic text but no panic/no-panic split, so it remains ruled out under the panic scope above. At that `6aaa91ac2b` revision, upstream `flaky.txt` still contained only `pydantic`, `scikit-build-core`, and `dd-trace-py`, which were already covered by the consistent-abort lane and confirmed PR `#4` / `#3` roots respectively. A live GitHub issue/PR search after the previous heartbeat found no newer nondeterminism discussion than the already-tracked issue and draft-PR set.
- A fresh exact-hash continuation on `6aaa91ac2b` widened that prepared-project sieve without relying on `ecosystem-analyzer` location grouping: `artigraph`, `hydra-zen`, `materialize`, `openlibrary`, `prefect`, `pylox`, `rich`, `scipy-stubs`, `setuptools`, `static-frame`, `strawberry`, `sympy`, `tornado`, and `xarray` each kept one non-panic exact and normalized concise hash for eight parallel full-project runs. The first wrapper attempt only printed valid stable results for `artigraph`, `materialize`, `openlibrary`, and `pylox` before failing on the wrong local `rich` checkout path; the corrected explicit-root rerun covered all fourteen projects. A live GitHub issue/PR search after that continuation again found no May 27 ty nondeterminism update beyond the already-tracked issue and draft-PR set.
- A fresh single-threaded checked-file entrypoint continuation on `6aaa91ac2b` used each prepared continuation project's lexicographically first and last tracked Python file before `.` to perturb Salsa query entry without Rayon scheduling. `artigraph`, `hydra-zen`, `materialize`, `openlibrary`, `prefect`, `pylox`, `rich`, `scipy-stubs`, `setuptools`, `static-frame`, `strawberry`, `sympy`, `tornado`, and `xarray` each kept one non-panic exact and normalized concise hash across those two explicit checked-file orders, so that prepared-project set does not currently add a dd-trace-style entrypoint repro either. A live GitHub issue/PR search after that entrypoint continuation again found no May 27 ty nondeterminism update beyond the already-tracked issue and draft-PR set.
- A fresh source-guided sweep of obvious remaining hash-iteration sites did not expose a new standalone root. `all_members()` still returns an `FxHashSet`, but user-facing completion candidates flow through `CompletionRanker`, which sorts by relevance, name, dependency kind, and module before returning results; the `KnownFunction::AllMembers` path sorts member names explicitly. `CheckMode::OpenFiles` still iterates an `FxHashSet<File>` when scheduling checks, but that only changes worker entry order and would be another trigger for the already-tracked cycle-entry family unless paired with a separate current repro.
- A diagnostic-collection follow-up did not expose a fourth root after `#5`: `TypeCheckDiagnostics` is vector-backed, suppression diagnostics are appended in source order, per-file diagnostics and project-collected diagnostics both sort through `Diagnostic::rendering_sort_key`, and diagnostic-adjacent hash collections in the checked path are used for membership/deduplication or canonicalized before visible output. The remaining `FxHashMap` handoff in fix mode is followed by the same rendering sort, so without a fresh repro it folds into the already-fixed equal-key reporter-order class rather than a new PR candidate.
- A follow-up audit of the old `#21686` unstable-iterator inventory on fresh `upstream/main` `a11c416b9c` also did not expose a new standalone root. The new upstream commits since `7c2e9fbc83` only touch dependency/action updates, not ty sources. The remaining current iterator sites from that inventory mostly fold into already-tracked lanes: constraint-set pair visitation is the active `#22578` source-order root; callable-return typevar renaming is the active `#24949` generic-callable root; project/open-file iteration is scheduling only unless it triggers a separate current cycle repro; `TypeCollector`, display guards, and suppression sets are membership-only; enum alias maps, protocol direct-member maps, enclosing-snapshot maps, star-import member snapshots, unpacker/inference expression maps, and `SpecializationBuilder.types` are keyed by source/AST IDs or consumed through ordered lookups/canonicalized output rather than raw type-key iteration. A raw literal-union helper in relation checking does iterate `FxHashSet<char>` / `FxHashSet<i64>`, but those scalar hashes are independent of Salsa IDs and their insertion sequence is fixed by the literal source, so it is not a same-source same-process nondeterminism lead without contrary evidence.
- A current Home Assistant `core` primer repro showed the same root cause without panics: repeated parallel full-project checks swapped two `invalid-argument-type` diagnostics at `tests/test_config_entries.py:5262:50` whose expected types were `Mapping[str, Any] | UndefinedType` and `MappingProxyType[str, tuple[DiscoveryKey, ...]] | UndefinedType`.
- Exact nondeterminism: non-panic diagnostics that share the same rendered source location, severity, and `DiagnosticId` could be printed in arrival order. In `core`, the two `invalid-argument-type` diagnostics had the same primary span and tied on error `DiagnosticId::InvalidArgumentType`, so repeated parallel runs preserved worker-completion order for visibly different lines.
- Root cause: `CollectReporter` receives checked-file diagnostics from Rayon workers and sorts them with `Diagnostic::rendering_sort_key`; the existing key compared primary file/range, severity, and ID but not the visible diagnostic message, so same-location same-ID diagnostics compared equal and Rust's stable sort preserved the race-dependent collection order.
- Fix direction for the third PR: keep the existing file/range, severity, and diagnostic-ID ordering, then use the concise rendered message as the final visible tie-breaker so same-key diagnostics sort canonically.
- Published the third confirmed fix as `jelle-openai/ruff#5`.
- PR `#5` follow-up: its remote head `9d1a654edf` still has the intended ordering fix but failed CI because affected mdtest snapshots were not refreshed, while the local branch now has validated follow-up commits `c819e31c32` and `86d5ea4eb7`. The refreshed `mro.md` and `subscript/assignment_diagnostics.md` focused mdtests pass locally, but publishing those commits still requires the explicit PushPatrol-bypass approval discussed earlier.
- Closed `jelle-openai/ruff#6` after the scope clarification above: it normalized exact output for an essentially identical deterministic panic, which is not a nondeterminism issue this investigation should pursue.
- Fresh `upstream/main` then advanced to `2428fca932` with `#25324`, which rejects annotations on non-name targets and only reported removed `unused-type-ignore-comment` diagnostics for `pyppeteer` in its ecosystem bot diff. The touched receiver-annotation path follows visible ancestor scopes and parameter order rather than a raw hash iteration. Rebuilding fresh `ty` there and replaying the concrete `pyppeteer` breadcrumb at checkout `7dc91ee5173d` found no new root: `pyppeteer/connection.py` kept one non-panic normalized concise hash for 100 parallel runs, and the whole project kept one non-panic normalized concise hash for eight parallel runs. A live May 27 ty nondeterminism issue/PR search still found no update beyond merged `#25417`.
- A fresh current-history pass on `2428fca932` found one old flake-detection breadcrumb not yet in the ledger: `#23178` reported `porcupine/pluginmanager.py:133` changing from `Unknown | str` to no diagnostic. Preparing `porcupine` at checkout `8e3407734531` and replaying current `ty` found no current root: that focused file kept one non-panic normalized concise hash for 100 parallel runs, and the whole project kept one non-panic normalized concise hash for eight parallel runs. The same tightened source sweep did not add a raw hash-consumer lead: protocol direct members are staged in an `FxHashMap` but canonicalized into a `BTreeMap` before `ProtocolInterface::members()` is visible, `CallArgumentTypes` only iterates context-keyed types to test exact/all-equal fallback or debug-display them, and unmatched signature keywords only iterate to ask whether any required parameter remains.
- Fresh `upstream/main` advanced again to `3cb09eba68` with metaclass-initialized class attributes (`#25342`) and `typing.TypeForm` (`#25334`), without changing the primer lists. `#25342`'s ecosystem bot diff named one `core` metaclass-result change at `homeassistant/components/bluetooth/passive_update_processor.py:97`; a freshly prepared current `core` checkout at `8184b93151d6` kept one non-panic exact concise hash `aa395dd1a39e` and one line-sorted concise hash `3643402906a9` for 100 runs of that file alone and 100 runs with `homeassistant/__init__.py` first. The new metaclass checker does iterate an `FxHashSet` of candidate member names, but those are string names collected from MRO/source-index walks and each name is checked independently before diagnostics are source-range sorted, so the stable `core` replay gives no separate root. `#25334`'s new TypeForm tests already include the review-requested nested type-context and overload shapes, and the touched TypeForm source only reused known membership caches plus ordered union/intersection iteration in the new visible paths. Open PR `#25240` adds initial `TypeVarTuple` support and has broad in-flight ecosystem diffs, but it is not on current `upstream/main`; do not treat it as current evidence or duplicate it without a fresh main-branch repro after it lands.
- Flake-detector caveat after inspecting pinned `astral-sh/ecosystem-analyzer@88eb3e1ceaf6`: its flaky classifier keys diagnostics by path, line, column, level, lint name, and message, so it does catch appearance/disappearance and diagnostic-content variants such as the live `Expression` lane. It deduplicates diagnostics within a run and sorts stable diagnostics after classification, so it does not catch a pure print-order permutation of an otherwise identical diagnostic multiset such as the already-confirmed `core` reporter-order root in PR `#5`. The scratch line-sorted digest harness has the same intentional blind spot; use exact concise-output digests when auditing reporter-order leads, and treat normalized-only stability as evidence only against content, panic, or return-code variants.
- A fresh current-main follow-up on merged `#24540` did not add a distinct pending-`SpecializationBuilder` root. Its ecosystem analyzer sample includes the already-live `Expression` constraint-content split now covered by active draft `#22578` and the already-confirmed dd-trace `patch.py:364` breadcrumb from PR `#3`; the remaining shown additions were ordinary changed diagnostics rather than flaky reports. Preparing current `boostedblob` at `cab5376deb85` and replaying the sample `boostedblob/boost.py:544` lane with exact concise-output hashing kept one non-panic raw/line-sorted hash for 100 focused runs (`2f8a1f6c320f` / `2e307ca08036`) and one non-panic raw/line-sorted hash for 100 runs with `boostedblob/__init__.py` first (`11900bd94a7f` / `8dd1fb5e2ed9`), so that adjacent pending-state change is recorded as active-upstream context rather than a new fork PR candidate.
- A fresh exact-output replay of the unreplayed `#23337` old flaky-list sample did not add a current union-display root on `3cb09eba68`. That PR's bot diff showed visible `Sized | Unknown` versus `Unknown | Sized` flips for `pip/src/pip/_internal/req/req_uninstall.py` and `spack/lib/spack/spack/llnl/util/filesystem.py`, plus `Unknown | str` versus `str | Unknown` flips for `pylint/checkers/refactoring/implicit_booleaness_checker.py`; preparing current `pip` at `8eb178480bd1` kept one non-panic raw/line-sorted hash for 100 focused runs and 100 runs with `src/pip/__init__.py` first (`e857c711a2d9` / `5ea9a312a929`). Preparing current `spack` at `ec758fd38589` kept one non-panic raw/line-sorted hash for 100 runs of `lib/spack/spack/detection/path.py` (`6f5968693d9f` / `1a03e9c354ca`), 100 runs of `lib/spack/spack/llnl/util/filesystem.py` (`fbda0086c12c` / `9610acad5214`), and 100 filesystem runs with `lib/spack/spack/__init__.py` first using that same filesystem hash. Preparing current `pylint` at `880b90d4afd3` kept one non-panic raw/line-sorted hash for 100 focused runs and 100 runs with `pylint/__init__.py` first (`c6edf57bbdbf` / `12a1d6680649`). Record those historical union-order breadcrumbs as cleared current-main evidence rather than a new fork PR candidate.
- The canonical closed `astral-sh/ty#2844` union-message repro is also cleared on fresh current main rather than a new fork PR lead. That issue showed `rich/tests/test_tools.py:17` flipping `Iterable[tuple[bool, str | Unknown]]` versus `Iterable[tuple[bool, Unknown | str]]` and `pylox/containers/array.py:11` flipping `deque[None | Unknown]` versus `deque[Unknown | None]`; preparing current `rich` at `46cebbb032f9` kept one non-panic raw/line-sorted hash for 100 focused runs and 100 runs with `rich/__init__.py` first (`a2c246cd26b0` / `e3d7d5df7466`). Preparing current `pylox` at `84da95eaa7f0` kept one non-panic raw/line-sorted hash for 100 focused runs and 100 runs with `pylox/__init__.py` first (`b1c6eddd87c0` / `e49244b4b6e4`), so the old source-order union-message symptom is recorded as stable under the corrected exact-output harness.
